{$SITE_ADDRESS} {
    encode zstd gzip

    header {
        -Server
        X-Content-Type-Options nosniff
        X-Frame-Options SAMEORIGIN
        Referrer-Policy strict-origin-when-cross-origin
    }

    @api path /api /api/*
    handle @api {
        reverse_proxy backend:8567
    }

    handle {
        reverse_proxy frontend:80
    }
}
